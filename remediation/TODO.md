# Remediation Flask Fix TODO

## Approved Plan Steps:
1. [x] Create `requirements.txt` with Flask dependency.
2. [x] Setup virtual environment: `cd remediation && python -m venv venv`
3. [] Activate venv and install deps: `venv\\Scripts\\activate && pip install -r requirements.txt`
4. [] Select venv interpreter in VSCode (Ctrl+Shift+P > Python: Select Interpreter)
5. [] Reload VSCode window to clear Pylance warning.
6. [] [Optional] Test local run: `python app.py`
7. [] Update TODO as steps complete.
